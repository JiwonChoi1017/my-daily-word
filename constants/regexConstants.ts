/** 空白チェック用正規表現. */
export const SPACE_CHECK_REGEX = /^\s*$/;

/** かなチェック用正規表現. */
export const KANA_CHECK_REGEX = /^[ぁ-んァ-ヶー]+$/;

/** カタカナチェック用正規表現. */
export const KATAKANA_CHECK_REGEX = /[ァ-ン]/g;
